{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | { read -r cmd; if echo \"$cmd\" | grep -q 'git push'; then npm run build > /dev/null 2>&1 || echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Build failed. Fix errors before pushing.\"}}'; fi; }",
            "timeout": 120,
            "statusMessage": "Verificando build antes do push..."
          }
        ]
      }
    ]
  }
}
